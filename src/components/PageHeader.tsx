import React from "react";

function PageHeader(props: {
  title: string,
  navigate: () => void,
  isFixed: boolean,
  isAbsolute: boolean,
}) {
  const { title, navigate, isFixed, isAbsolute } = props;
  return <div />
};

export default PageHeader;